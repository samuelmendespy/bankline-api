## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL jdbc:SoftwaredeBancoDeDados://servidor:porta/bancoDeDados
spring.datasource.url=jdbc:postgresql://localhost:5432/banklineapi_db
spring.datasource.username=gm
spring.datasource.password=adminpw

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Resolver problema de Apache estar usando a mesma porta do TOmcatWeb server
# server.port=8888
